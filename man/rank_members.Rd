% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_members.R
\name{rank_members}
\alias{rank_members}
\title{Rank members by bipartisanship score}
\usage{
rank_members(
  congress,
  chamber,
  score_type = c("attract", "offer"),
  party = NULL,
  issue = NULL,
  n = 10,
  descending = TRUE
)
}
\arguments{
\item{congress}{Integer. Congress number (e.g., \code{117}).}

\item{chamber}{Character. \code{"house"} or \code{"senate"}.}

\item{score_type}{Character. \code{"attract"} (default) or \code{"offer"}.}

\item{party}{Optional character. \code{"D"} or \code{"R"} to restrict
to one party.}

\item{issue}{Optional character. If supplied, ranks by this issue-area
score instead of the general score. See \code{\link[=list_issues]{list_issues()}} for valid
labels.}

\item{n}{Integer. Number of members to return. Default \code{10}.
Pass \code{Inf} to return all.}

\item{descending}{Logical. Rank highest scores first (default), or
lowest-first if \code{FALSE}.}
}
\value{
A tibble with columns \code{rank}, \code{name}, \code{party},
\code{bioguide_id}, and the score column used for ranking.
}
\description{
Returns a leaderboard of members sorted by either the attract or offer
Portman score within a specified Congress and chamber. Optionally restrict
to a party, or rank by an issue-area score instead of the general score.
}
\examples{
# Top 10 most-attracting senators in the 117th
rank_members(117, "senate", "attract")

# Top 5 House Republicans offering the most bipartisanship
rank_members(117, "house", "offer", party = "R", n = 5)

# Senators ranked by health-policy attract score
rank_members(117, "senate", "attract", issue = "health")

}
\seealso{
\code{\link[=get_member_scores]{get_member_scores()}}, \code{\link[=get_issue_scores]{get_issue_scores()}}
}
