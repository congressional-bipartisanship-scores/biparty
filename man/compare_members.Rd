% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_members.R
\name{compare_members}
\alias{compare_members}
\title{Compare bipartisanship scores for multiple members}
\usage{
compare_members(
  identifiers,
  congress = NULL,
  chamber = NULL,
  score_type = c("both", "attract", "offer")
)
}
\arguments{
\item{identifiers}{Character vector of Bioguide IDs or name substrings.
Must contain at least two entries.}

\item{congress}{Optional integer vector. Restrict to these Congress numbers.}

\item{chamber}{Optional character. \code{"house"} or \code{"senate"}.}

\item{score_type}{Character. \code{"attract"}, \code{"offer"}, or
\code{"both"} (default). Controls which score columns are returned.}
}
\value{
A tibble with columns \code{congress}, \code{chamber},
\code{bioguide_id}, \code{name}, \code{party}, and the requested score
column(s), sorted by Congress, chamber, and name.
}
\description{
Returns a single tidy tibble with general bipartisanship scores for two or
more members side by side. Each member is looked up via \code{\link[=get_member_scores]{get_member_scores()}},
so identifiers can be a mix of Bioguide IDs and name substrings.
}
\examples{
compare_members(c("P000449", "M001183"), congress = 117)
compare_members(c("P000449", "manchin"), congress = 117)
compare_members(c("P000449", "M001183"), score_type = "attract")

}
\seealso{
\code{\link[=get_member_scores]{get_member_scores()}}, \code{\link[=rank_members]{rank_members()}}, \code{\link[=plot_trend]{plot_trend()}}
}
