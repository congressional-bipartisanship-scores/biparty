% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_state_delegation.R
\name{get_state_delegation}
\alias{get_state_delegation}
\title{Get scores for an entire state delegation}
\usage{
get_state_delegation(
  state,
  congress = NULL,
  chamber = NULL,
  party = NULL,
  aggregate = FALSE
)
}
\arguments{
\item{state}{Character. Two-letter state abbreviation (e.g., \code{"OH"},
\code{"CA"}). Case-insensitive.}

\item{congress}{Optional integer vector. Restrict to these Congresses.}

\item{chamber}{Optional character. \code{"house"} or \code{"senate"}.}

\item{party}{Optional character. \code{"D"} or \code{"R"}.}

\item{aggregate}{Logical. If \code{TRUE}, collapse to a single-row
summary with mean scores. Default \code{FALSE}.}
}
\value{
A tibble of member-level scores (if \code{aggregate = FALSE}) or
a one-row summary tibble with columns \code{state}, \code{n_members},
\code{n_observations}, \code{mean_attract}, and \code{mean_offer}
(if \code{aggregate = TRUE}).
}
\description{
Returns bipartisanship scores for all members who served from a given
state, optionally filtered by Congress, chamber, and party. State
membership is determined from the \code{state} column embedded in
\link{aggregate.cbs}.
}
\examples{
get_state_delegation("OH", congress = 117)
get_state_delegation("OH", chamber = "senate")
get_state_delegation("CA", congress = 117, aggregate = TRUE)

}
\seealso{
\code{\link[=get_member_scores]{get_member_scores()}}, \code{\link[=rank_members]{rank_members()}}
}
