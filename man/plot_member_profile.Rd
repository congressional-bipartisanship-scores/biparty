% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_member_profile.R
\name{plot_member_profile}
\alias{plot_member_profile}
\title{Plot a member's bipartisanship profile across all issue areas}
\usage{
plot_member_profile(
  identifier,
  congress = NULL,
  chamber = NULL,
  score_type = c("attract", "offer"),
  drop_issues = c("commem", "history", "na", "private"),
  show_photo = TRUE,
  title = NULL
)
}
\arguments{
\item{identifier}{Character. Bioguide ID or name substring.}

\item{congress}{Integer. Congress to display. Defaults to the member's
most recent Congress.}

\item{chamber}{Optional character. \code{"house"} or \code{"senate"}.}

\item{score_type}{Character. \code{"attract"} (default) or \code{"offer"}.}

\item{drop_issues}{Character vector of short issue labels to exclude.}

\item{show_photo}{Logical. Attempt to fetch congressional headshot.
Requires \pkg{magick}. Default \code{TRUE}.}

\item{title}{Optional character. Figure title.}
}
\value{
A \code{ggplot} object or \pkg{patchwork} composite.
}
\description{
Creates a bar chart of attract or offer scores across all CRS policy areas
for a given Congress. When the \pkg{magick} and \pkg{patchwork} packages
are installed, the output is a rich profile card with a photo, info card,
and bar chart.
}
\examples{
plot_member_profile("P000449")

\donttest{
plot_member_profile("P000449", congress = 117)
plot_member_profile("portman", score_type = "offer")
}

}
\seealso{
\code{\link[=get_issue_scores]{get_issue_scores()}}, \code{\link[=plot_bipartisanship]{plot_bipartisanship()}}, \code{\link[=list_issues]{list_issues()}}
}
