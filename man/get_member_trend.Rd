% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_member_trend.R
\name{get_member_trend}
\alias{get_member_trend}
\title{Return a member's bipartisanship scores across Congresses}
\usage{
get_member_trend(
  identifier,
  chamber = NULL,
  score_type = c("both", "attract", "offer")
)
}
\arguments{
\item{identifier}{Character. Bioguide ID or name substring
(see \code{\link[=get_member_scores]{get_member_scores()}}).}

\item{chamber}{Optional character. \code{"house"} or \code{"senate"}.}

\item{score_type}{Character. \code{"attract"}, \code{"offer"}, or
\code{"both"} (default). Controls which score columns are returned.}
}
\value{
A tibble with columns \code{congress}, \code{chamber},
\code{bioguide_id}, \code{name}, \code{party}, and the requested score
column(s), ordered by Congress.
}
\description{
A convenience wrapper around \code{\link[=get_member_scores]{get_member_scores()}} that returns a tidy time series
of attract and/or offer scores, ordered by Congress. Suitable for passing
directly to \code{\link[=plot_trend]{plot_trend()}} or for custom longitudinal analysis.
}
\examples{
# Rob Portman's full attract/offer history
get_member_trend("P000449")

# Attract scores only
get_member_trend("portman", score_type = "attract")

# Senate career only
get_member_trend("P000449", chamber = "senate")

}
\seealso{
\code{\link[=get_member_scores]{get_member_scores()}}, \code{\link[=plot_trend]{plot_trend()}}
}
